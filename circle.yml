dependencies:
  post:
  - sudo pip install gcovr
  - sudo pip install cpp-coveralls
  - make compile

test:
  post:
    - make test
    - make coverage-xml
    - wc -l output.txt
    - tail -n 30 output.txt

